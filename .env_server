GROUNDSTATION_MODE=lab
# ── TimescaleDB / Postgres ──
POSTGRES_USER=postgres
POSTGRES_PASSWORD='password'
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD='password'
PGDATABASE=postgres
PGSSL=false

# ── Grafana ──
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=les-password

# ── pgAdmin ──
PGADMIN_DEFAULT_EMAIL=leos@leos.ece.gatech.edu
PGADMIN_DEFAULT_PASSWORD='password'

# ── Ingest API ──
INGEST_PORT=4000
INGEST_URL=http://localhost:4000

# ── File path ──
LAUNCH_DATA_FILE=./launch_data.txt

UID=1001
GID=1002