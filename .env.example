# Common
GROUNDSTATION_MODE=lab
INGEST_PORT=4000
INGEST_URL=http://localhost:4000
SPOOL_DB_PATH=./spool.db

# Pi-only fake/replay input
FAKE_RADIO_INPUT_PATH=
FAKE_RADIO_REPEAT=false
FAKE_RADIO_INTERVAL_S=0

# TimescaleDB / Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=changeme
PGDATABASE=postgres
PGSSL=false

# Grafana
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme

# pgAdmin
PGADMIN_DEFAULT_EMAIL=you@example.com
PGADMIN_DEFAULT_PASSWORD=changeme
